import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.duin.argconvert',
  appName: 'Currency Converter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
